# Nyora Web Upgrade Backlog

- [x] WB-001 | Web sync -> new stream backend | repo:web | ✓ Migrated core/sync.js off Supabase to https://stream.hasanraza.tech: OAuth2 email/password (POST /auth/register JSON + /auth/token form grant_type=password|refresh_token), Bearer-only /functions/v1/nyora-sync (apikey/anon header dropped), auto-refresh+retry on 401. Removed Google Identity Services flow; added signIn/register. env.js now NYORA_SYNC_URL (Supabase/Google vars removed). settings.js + welcome.js sign-in UIs switched to email/password fields; row shapes/local data model preserved. Build green + node --check clean. Migrate web/core/sync.js OFF old Supabase (fqguzcoytnbnjwaddakn.supabase.co + anon key/id_token grant) to the self-hosted sync server at https://stream.hasanraza.tech. Auth: POST /auth/register {email,password} + POST /auth/token (form: grant_type=password&username=<email>&password=, and grant_type=refresh_token&refresh_token=) -> {access_token, refresh_token, ...}; persist tokens; auto-refresh on 401. Sync: POST /functions/v1/nyora-sync with Authorization: Bearer <access_token>, body {"action":"upsert","table":"<t>","rows":[...]} and {"action":"select","table":"<t>","since":"<ISO>"}. Drop the apikey/anon header. Tables (canonical snake_case): nyora_manga, nyora_favourite, nyora_history, nyora_bookmark, nyora_category, nyora_manga_category, nyora_manga_prefs, nyora_source_prefs, nyora_update, nyora_tracking. Update web/env.js: add NYORA_SYNC_URL='https://stream.hasanraza.tech' (keep/remove Supabase vars as needed). Preserve the existing local library data model + row shapes.
- [x] WB-002 | Web helper -> api.hasanraza.tech | repo:web | ✓ core/api.js now prefers the hosted helper at NYORA_HELPER_URL (https://api.hasanraza.tech), added to env.js. Low-level get() routes /sources/catalog|popular|latest|search|details|pages to the helper FIRST (helperGet), falling back to the in-browser web parsers (parser-runtime) then same-origin; details()/pages() switched to /sources/details & /sources/pages (parser-runtime.handle now aliases those to /manga/*). imageUrl() builds <helper>/image?u=<enc>&h=k:v (was the CORS worker). /sources/install fires a best-effort helper POST while local prefs stay authoritative. Build green + node --check clean. Ensure web/core/api.js (content/parser client) targets the hosted helper https://api.hasanraza.tech for: /sources/catalog, /sources/popular?id=&page=, /sources/latest?id=&page=, /sources/search?id=&q=&page=, /sources/details, /sources/pages, /image?u=<enc>, /sources/install. Add NYORA_HELPER_URL='https://api.hasanraza.tech' in env.js if not present. Keep client-side web-parsers as a fallback only if already wired; prefer the hosted helper.
- [x] WB-003 | Discover home screen | repo:web | ✓ Added web/screens/discover.js — android-style Discover home: persistent top search bar (submits a universal search->search screen), a HERO card built from the #1 AniList trending manga (blurred banner backdrop + real cover + title + score/genre chips + a Read button), a "Trending now" horizontal snap-rail (Show all), and an "All-time popular" horizontal rail (Show all). Feed = single no-auth POST to https://graphql.anilist.co with aliased Page queries (TRENDING_DESC + POPULARITY_DESC) pulling title/coverImage/bannerImage/genres/description/averageScore; AniList CORS-friendly, covers load direct via applyImage with proxy fallback. Tapping any entry (hero or rail card) runs router.navigate('search',{q:title}) = universal search across installed sources; Show all -> suggestions grid. Registered route+meta+nav item in app.js; added .discover* CSS (hero, snap rails, mobile breakpoint) in styles.css. Hero/rail skeletons + errorBox+Retry + per-render token guard. Build green (bundle 64a32fa2) + node --check clean. Add web/screens/discover.js — an android-style Discover home (see nyora-android screenshots): a top search bar, a HERO card (top trending manga + genres + Read button), a "Trending now" horizontal rail (with a "Show all"), and an "All-time popular" rail (Show all). Source the feed from AniList GraphQL (https://graphql.anilist.co, no auth: Page.media(type:MANGA, sort:[TRENDING_DESC] and [POPULARITY_DESC]) -> title, coverImage.large [open CDN], genres, description) — mirror the iOS Discover. Tapping an entry runs a universal search across installed sources to open it.
- [x] WB-004 | Floating pill bottom nav (mobile web) | repo:web | ✓ Rebuilt the mobile bottom nav in app.js as a floating rounded pill (.tabbar-pill: centred, max-width 420px, 999px radius, surface1 bg + hairline border + shadow + blur) holding the 4 android-style primary destinations — Discover / Library / Explore / History (TABBAR_KEYS) — with a detached circular accent "continue reading" reader FAB (#readerFab, 56px, calls continueReading()) floating to its right. .tabbar is now a transparent pointer-events:none flex container (bottom = safe-area inset) so only the pill+FAB are interactive; active tab uses accent. Default landing screen + mobileBrand now route to 'discover' (was 'suggestions'). node --check clean + build green (bundle bb0b9d31). Restructure the web app's navigation into a floating rounded pill bottom bar matching android: Discover / Library / Explore / History, PLUS a detached circular "continue reading" reader FAB to its right. This is the MOBILE-WEB design (responsive; primary target is a phone browser). Make Discover the default landing screen.
- [x] WB-005 | Explore screen -> android layout | repo:web | ✓ Rebuilt web/screens/explore.js as an android-style Explore landing: a universal search field (submits -> search screen), a 2x2 quick-actions card (Local storage->local, Bookmarks->bookmarks, Random->random-manga action, Downloads->downloads), then a "Manga sources" grid of installed-source tiles with a "Catalog" action (opens the extensions/catalog sheet, empty-state has an "Open catalog" CTA). Random picks a random installed source + a random popular title and navigates to details. Tapping a source tile swaps the screen into the classic per-source browse view (Popular/Latest/Search grid) with a Back button in the header returning to the landing; the mobile source pill still opens the bottom-sheet source list. Preserved the token-guarded browse state machine + sources-synced refresh. Added .explore-home/.quick-actions/.quick-action/.source-grid/.source-tile CSS + mobile back-button+pill row tweaks in styles.css. Build green (bundle 8662d373) + node --check clean. Restyle web/screens/explore.js to match android Explore: search field, a 2x2 quick-actions card (Local storage, Bookmarks, Random, Downloads), then a "Manga sources" grid (source icons) with a "Catalog" button. Wire the 4 quick actions to the existing web screens (local.js, bookmarks.js, a random-manga action, downloads.js).
- [x] WB-006 | Library = Favourites + Downloads subsections | repo:web | ✓ Restructured web/screens/library.js into an android-style two-subsection Library. Added a top segmented toggle (Favourites | Downloads) below the page title; the ?tab=downloads param can deep-link to Downloads. Favourites subsection keeps the existing sort row + category chips + grid, with the android empty state "It's kind of empty here…" (Explore CTA retained). Downloads subsection lazily mounts downloads.js's render() into its own host on first visit and bridges dlSection.__downloadsTeardown up to the library view so app.js drops the download-manager subscription on navigate-away; the favourites title-count is hidden while on Downloads. Sections toggle via display none/'' so state is preserved when switching tabs. Build green (bundle b4226f0b) + node --check clean. Restructure web/screens/library.js so Library has two subsections/tabs: Favourites (favourite categories, android-style empty state "It's kind of empty here…") and Downloads (from downloads.js). Match the android look. web/screens/library.js so Library has two subsections/tabs: Favourites (favourite categories, android-style empty state "It's kind of empty here…") and Downloads (from downloads.js). Match the android look.
- [x] WB-007 | Mobile-web visual polish (android look) | repo:web | ✓ Re-skinned the mobile-web design system to mirror nyora-android. Type: added Poppins (Google Fonts) as the leading --font-display + --font-body face (Clash/Satoshi kept as fallbacks) so the UI reads like the android product face. Dark palette: swapped the flat neutral-black tokens for android's cosmic-midnight blue-tinted set (bg #060810, bg-elev #0d1222, surface1 #131b32, surface2 #1a2442, surface3 #26335c, indigo-tinted hairlines/card-borders, accent-tinted bg-tint gradient); sidebar base now uses --bg (was pure-black). Light palette: nudged to android warm slate (bg #f1f5f9, elev #f8fafc, surfaces #e2e8f0/#cbd5e1). Bottom nav: active destination now shows a Material-3 indigo pill (accent-soft bg) and the reader FAB carries an accent-glow shadow. Updated theme-color metas + manifest bg/theme to #060810, bumped styles.css cache-bust to v=20. Accent was already indigo (#6366F1). Build green (bundle b4226f0b), dist assets verified. Apply the android/nyora visual language across the mobile-web UI: indigo accent, rounded flat cards, Poppins-style type, card spacing, hero/rail styling — so it reads like the android app screenshots. Keep it responsive/mobile-first.
- [ ] WB-008 | Android Library: Favourites + Downloads subsections | repo:android | In nyora-android, make the Library screen (favourites/ui/container/FavouritesContainerFragment + FavouritesContainerAdapter + FavouritesTabConfigurationStrategy) include BOTH Favourites AND Downloads as subsections. Currently the Library is a ViewPager/TabLayout of favourite CATEGORIES only; add a Downloads subsection (reuse download/ui/list/DownloadsListFragment) so the Library groups Favourites + Downloads. Keep it building.
